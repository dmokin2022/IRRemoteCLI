
// Таблица переменных доступных для вывода через командную строку

accessible_variable_t accessible_variables_table[] = {
    VAR(var1),
    VAR(var2),
    VAR(var3),
    VAR(var4),
    STR(str1),
};
